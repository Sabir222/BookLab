export default function DashLayout({ children }: { children: React.ReactNode }) {
        return (
                <html lang="en">
                        <body>
                                test
                                {children}
                        </body>
                </html>
        );
}
